import { DirectiveHighlightPipe } from './directive-highlight.pipe';

describe('DirectiveHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new DirectiveHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
